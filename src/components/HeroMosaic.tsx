"use client";

/**
 * HeroMosaic — deprecated.
 *
 * The loud multicolor shard-mosaic hero backdrop has been retired in favor of the
 * single global greyscale Voronoi field (see `Background.tsx`, mounted once in
 * layout). This component is no longer rendered anywhere; the export is kept only
 * so any stray importer still compiles. It paints nothing.
 */
export function HeroMosaic() {
  return null;
}
